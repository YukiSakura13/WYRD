# Legacy Forest production derivatives

These files are inactive conversions formerly stored beside the live Forest
artwork. They are retained only for history and must not be linked from the app.

The active production folder is `assets/images/forest-home/silver/`. It contains
one file per Forest card.
