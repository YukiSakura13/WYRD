# Retired gold UI icons

These book, card, sound, and bell assets belong to the retired gold interface.
They are kept only as project history and are not runtime dependencies.

Active Silver interface glyphs live in `assets/ui/icons/`. Approved button
geometry lives in `assets/ui/action-buttons/continuous/`.
