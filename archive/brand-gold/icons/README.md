# Archived gold/owl identity assets

These files are the former production favicon, Apple Touch, PWA, source, and
square owl assets from the gold brand direction. They are retained for project
history and are no longer referenced by the active app, manifest, or Silver UI
Kit.

The active external mark is the letter-free `Thorn ring` Forest Seal stored in
`public/` and `public/icons/`.
